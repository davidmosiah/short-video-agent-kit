# Security Policy

Do not include provider API keys, generated private prompts or unreleased campaign assets in issues. Redact sensitive data before sharing logs.

Dry-run is enabled by default. Paid generation requires an explicit live mode.
